<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - DnTech</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="~/css/auth.css" asp-append-version="true" />
</head>
<body class="@ViewData["BodyClass"]">
    <header>
        <nav class="navbar navbar-expand-lg navbar-custom">
            <div class="container">
                <a class="navbar-brand" asp-area="" asp-page="/Index">
                    <i class="bi bi-shield-lock"></i>
                    <strong>DnTech</strong>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto align-items-lg-center">
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-page="/Index">
                                <i class="bi bi-house-door me-1"></i>
                                Inicio
                            </a>
                        </li>

                        @* Mostrar solo cuando NO esté autenticado *@
                        @if (string.IsNullOrEmpty(Context.Request.Cookies["AuthToken"]))
                        {
                            <li class="nav-item ms-lg-2">
                                <a class="btn btn-outline-light" asp-area="" asp-page="/Auth/Login">
                                    <i class="bi bi-box-arrow-in-right me-1"></i>
                                    Iniciar Sesión
                                </a>
                            </li>
                            <li class="nav-item ms-lg-2">
                                <a class="btn btn-light" asp-area="" asp-page="/Auth/Register">
                                    <i class="bi bi-person-plus me-1"></i>
                                    Registrarse
                                </a>
                            </li>
                        }
                        else
                        {
                            @* Mostrar cuando esté autenticado *@
                            <li class="nav-item">
                                <span class="nav-link user-badge">
                                    <i class="bi bi-person-circle me-1"></i>
                                    @Context.Request.Cookies["UserName"]
                                </span>
                            </li>
                            <li class="nav-item ms-lg-2">

                                <button type="button" class="btn btn-outline-light" data-bs-toggle="modal" data-bs-target="#logoutModal">
                                    <i class="bi bi-box-arrow-right me-1"></i>
                                    Cerrar Sesión
                                </button>
                                
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <!-- Modal de Confirmación para Cerrar Sesión -->
    <div class="modal fade" id="logoutModal" tabindex="-1" aria-labelledby="logoutModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header border-bottom-0">
                    <h5 class="modal-title" id="logoutModalLabel">
                        <i class="bi bi-question-circle-fill text-primary me-2"></i>
                        Confirmar cierre de sesión
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body text-center py-4">
                    <div class="mb-3">
                        <i class="bi bi-box-arrow-right" style="font-size: 3rem; color: var(--primary-color);"></i>
                    </div>
                    <h5 class="mb-3">¿Estás seguro de que deseas cerrar sesión?</h5>
                    <p class="text-muted mb-0">
                        Serás redirigido a la página de inicio y deberás iniciar sesión nuevamente para acceder a tu cuenta.
                    </p>
                </div>
                <div class="modal-footer border-top-0 justify-content-center">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
                        <i class="bi bi-x-circle me-1"></i>
                        Cancelar
                    </button>
                    <form id="logoutForm" method="post" asp-page="/Auth/Logout" style="display: inline;">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-box-arrow-right me-1"></i>
                            Sí, cerrar sesión
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>


    <main role="main">
        @RenderBody()
    </main>

    <footer class="footer-custom">
        <div class="container text-center">
            <p>
                &copy; @DateTime.Now.Year <strong>DnTech</strong> - Sistema de Autenticación con Clean Architecture
                <span class="mx-2">|</span>
                <a asp-area="" asp-page="/Privacy">Política de Privacidad</a>
            </p>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>